package com.oluwaseyi.Bank_Demo_Backend.config;



public class JwtKeyGenerator {

    // public static void main(String[] args) throws Exception {
    //     // Generate a 256-bit (32-byte) secret key
    //     KeyGenerator keyGen = KeyGenerator.getInstance("HmacSHA256");
    //     keyGen.init(256);
    //     SecretKey secretKey = keyGen.generateKey();

    //     // Encode the secret key to a string using Base64
    //     String encodedKey = Base64.getEncoder().encodeToString(secretKey.getEncoded());

    //     // Set the expiration time (e.g., 10 hours in milliseconds)
    //     long expirationTime = 36000000L; // 10 hours

    //     // Print the secret key and expiration time
    //     System.out.println("Generated JWT Secret Key (Base64 Encoded): " + encodedKey);
    //     System.out.println("Expiration Time (in milliseconds): " + expirationTime);
    // }
}
